## `res` directory

This directory contains "resources", i.e., images and graphics, used in the main README.

It's split into two subdirectories:

-   `dark`: contains resources for dark mode.
-   `light`: contains resources for light mode.
